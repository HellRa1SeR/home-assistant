kiosk_mode:
  user_settings:
    - users:
        - kiosk
      kiosk: false
decluttering_templates:
  mushroom_title_card:
    default:
      - title: ""
    card:
      type: custom:mushroom-title-card
      title: "[[title]]"
      card_mod:
        style: |
          ha-card {
            --title-font-size: 20px !important;
          }
  mushroom_room_title_card:
    default:
      - title: ""
    card:
      type: custom:mushroom-title-card
      title: "[[title]]"
      card_mod:
        style: |
          ha-card {
            --title-font-size: 35px !important;
          }          
  mushroom_title_subtitle_card:
    default:
      title: ""
      subtitle: ""
    card:
      type: custom:mushroom-title-card
      title: "[[title]]"
      subtitle: "[[subtitle]]"
      card_mod:
        style: |
          ha-card {
            --title-font-size: 20px !important;
            --subtitle-font-size: 14px !important;
          }
  room_summary_card:
    description: Primary room info (name + temp/humidity)
    variables:
      - room_name
      - temperature_sensor
      - humidity_sensor
      - navigation_path
    card:
      type: custom:mushroom-template-card
      primary: "[[room_name]]"
      secondary: >-
        {{ states('[[temperature_sensor]]') | round(1) }}°C / {{
        states('[[humidity_sensor]]') | round(1) }}%
      tap_action:
        action: navigate
        navigation_path: "[[navigation_path]]"
      hold_action:
        action: none
      double_tap_action:
        action: none
      card_mod:
        style:
          .: |
            ha-card {
              --primary-text-color: black;
              border: 0px;
              padding-left: 5px;
              padding-top: 5px;
              width: 500px;
            }
          mushroom-state-info$: |
            .container {
              --card-primary-color: var(--primary-text-color);
              --card-secondary-color: var(--primary-text-color);
            }
  big_icon_toggle_card:
    description: Large horizontal icon toggle for switch/light
    variables:
      - entity
      - icon
    card:
      type: custom:mushroom-template-card
      entity: "[[entity]]"
      layout: horizontal
      icon: "[[icon]]"
      icon_color: |-
        {% if is_state(entity, 'on') %} 
          #ecce85
        {% else %}
          #5b5b5b
        {% endif %}      
      tap_action:
        action: toggle
      hold_action:
        action: none
      double_tap_action:
        action: none
      primary_info: none
      secondary_info: none
      card_mod:
        style:
          ha-tile-icon$: |
            .container {
              border: 0px;
              width: 120px !important;
              height: 120px !important;
              border-radius: 50% !important;
                }   
          .: |
            ha-tile-icon {
              --mdc-icon-size: 90px !important;
               }
            ha-card {
              border: none;
              margin-top: -8px !important;
              margin-left: -24px !important;
               }
  light-off-percentage-button:
    default:
      - entity: light.example
    card:
      type: custom:my-slider-v2
      entity: "[[entity]]"
      allowTapping: false
      allowSliding: true
      styles:
        container:
          - border-radius: 30px
        card:
          - margin-top: "-8px"
          - height: 75px
        progress:
          - background: none
        thumb:
          - width: 3px
          - height: 30%
          - background: var(--primary-text-color)
          - border-radius: 5px
          - top: 35%
          - margin-right: 12px
        track:
          - background: none
  light-on-percentage-button:
    default:
      - entity: light.example
    card:
      type: custom:my-slider-v2
      entity: "[[entity]]"
      allowTapping: false
      allowSliding: true
      styles:
        container:
          - border-radius: 30px
        card:
          - margin-top: "-8px"
          - height: 75px
        progress:
          - background: var(--gradient-progress)
        thumb:
          - width: 3px
          - height: 30%
          - background: var(--primary-text-color)
          - border-radius: 5px
          - top: 35%
        track:
          - background: var(--gradient-track)
      card_mod:
        style: |
          :host {
            --theme-primary-color: var(--md-sys-color-primary);
            --gradient-track: color-mix(in srgb, var(--md-sys-color-primary) 70%, transparent);
            --gradient-progress: var(--md-sys-color-primary);
          }
  open_window_title_card:
    description: Title showing "Open Window(s)" if any are open
    variables:
      - windows_entity
    card:
      type: custom:mushroom-title-card
      title: >-
        {% set open_windows = expand(states['[[windows_entity]]'])
            | selectattr('state', 'eq', 'on')
            | list | count %}
        {{ '' if open_windows == 0 else ('Open Window' if open_windows == 1 else
        'Open Windows') }}
      card_mod:
        style: |
          ha-card {
            --title-font-size: 20px !important;
          }
  open_window_chip_card:
    description: Horizontal chip list showing open windows
    variables:
      - windows_entity
    card:
      type: horizontal-stack
      cards:
        - type: custom:mushroom-chips-card
          chips:
            - type: conditional
              conditions:
                - condition: state
                  entity: "[[windows_entity]]"
                  state: "on"
              chip:
                type: template
                content: |
                  {{ expand(states['[[windows_entity]]']) 
                      | selectattr('state', 'eq', 'on') 
                      | map(attribute='name') 
                      | join(', ') }}
                icon: mdi:window-open
                icon_color: orange
  open_door_window_title_card:
    description: Shows dynamic title for open doors/windows (with plural logic)
    variables:
      - window_entity
      - door_entity
    card:
      type: custom:mushroom-title-card
      title: |-
        {% set windows_open = expand(states['[[window_entity]]'])
            | selectattr('state', 'eq', 'on') | list | count %}
        {% set doors_open = expand(states['[[door_entity]]'])
            | selectattr('state', 'eq', 'on') | list | count %}

        {% if windows_open > 0 and doors_open > 0 %}
          {{ 'Open Windows and Doors' if windows_open > 1 and doors_open > 1 else 
             'Open Window and Doors' if windows_open == 1 and doors_open > 1 else 
             'Open Windows and Door' if windows_open > 1 and doors_open == 1 else 
             'Open Window and Door' }}
        {% elif windows_open > 0 %}
          {{ 'Open Windows' if windows_open > 1 else 'Open Window' }}
        {% elif doors_open > 0 %}
          {{ 'Open doors' if doors_open > 1 else 'Open Door' }}
        {% else %}
          ''
        {% endif %}
      card_mod:
        style: |
          ha-card {
            --title-font-size: 20px !important;
          }
  open_door_window_chip_card:
    description: Chips showing open doors and windows (if any)
    variables:
      - window_entity
      - door_entity
    card:
      type: custom:mushroom-chips-card
      chips:
        - type: conditional
          conditions:
            - condition: state
              entity: "[[door_entity]]"
              state: "on"
          chip:
            type: template
            content: |
              {{ expand(states['[[door_entity]]'])
                  | selectattr('state', 'eq', 'on') 
                  | map(attribute='name') 
                  | join(', ') }}
            icon: |-
              {% set open_doors = expand(states['[[door_entity]]']) 
                  | selectattr('state', 'eq', 'on') 
                  | list | count %}
              {{ 'mdi:door-sliding-open' if open_doors > 0 else '' }}
            icon_color: red
        - type: conditional
          conditions:
            - condition: state
              entity: "[[window_entity]]"
              state: "on"
          chip:
            type: template
            content: |
              {{ expand(states['[[window_entity]]']) 
                  | selectattr('state', 'eq', 'on') 
                  | map(attribute='name') 
                  | join(', ') }}
            icon: mdi:window-open
            icon_color: orange
  waterfall_temp_hum:
    card:
      type: custom:waterfall-history-card
      hours: 24
      height: 25
      compact: true
      entities:
        - entity: "[[temp_entity]]"
          name: "[[temp_name]]"
          thresholds:
            - value: 16
              color: "#CEB2F5"
            - value: 18
              color: "#5EBDEE"
            - value: 21
              color: "#9cc8b8"
            - value: 24
              color: "#e7b562"
            - value: 27
              color: "#FF564B"
            - value: 35
              color: "#99332d"
        - entity: "[[hum_entity]]"
          name: "[[hum_name]]"
          show_labels: true
          thresholds:
            - value: 0
              color: "#99332d"
            - value: 31
              color: "#228B22"
            - value: 51
              color: "#99332d"
button_card_templates:
  light_on_button:
    tap_action:
      action: none
    styles:
      grid:
        - grid-template-columns: 64px 1fr
        - grid-template-areas: "\"i n\""
      card:
        - background: none
        - height: 75px
        - position: absolute
        - z-index: 1
      img_cell:
        - width: 22px
      icon:
        - width: 22px
        - color: var(--md-sys-color-on-primary)
      name:
        - justify-self: start
        - font-size: 14px
        - font-weight: 400
        - color: var(--md-sys-color-on-primary)
  light_off_button:
    tap_action:
      action: none
    styles:
      grid:
        - grid-template-columns: 64px 1fr
        - grid-template-areas: "\"i n\""
      card:
        - background: >-
            color-mix(in srgb, var(--md-sys-color-surface-variant) 40%,
            transparent)
        - height: 75px
        - position: absolute
        - z-index: 1
      img_cell:
        - width: 22px
      icon:
        - width: 22px
        - color: var(--primary-text-color)
      name:
        - justify-self: start
        - font-size: 14px
        - font-weight: 400
        - color: var(--primary-text-color)
  standard_button:
    tap_action:
      action: toggle
    styles:
      grid:
        - grid-template-columns: 64px 1fr
        - grid-template-areas: "\"i n\""
      card:
        - background: >-
            color-mix(in srgb, var(--md-sys-color-surface-variant) 40%,
            transparent)
        - height: 75px
      img_cell:
        - width: 22px
      icon:
        - width: 22px
        - color: var(--primary-text-color)
      name:
        - justify-self: start
        - font-size: 14px
        - font-weight: 400
        - color: var(--primary-text-color)
    state:
      - value: "on"
        styles:
          card:
            - background: var(--md-sys-color-primary)
          icon:
            - color: var(--md-sys-color-on-primary)
          name:
            - color: var(--md-sys-color-on-primary)
views:
  - title: Overview
    type: custom:grid-layout
    layout:
      grid-template-columns: 25% 25% 25% 25%
      grid-template-rows: auto
      grid-template-areas: |
        "title1 title2 title3 title4"
        "title1 title2 title3 title4"
        "title11 title22 title33 title4"     
        "left3 left4 middle3 title4"
        "title5 title6 title7 title8"        
        "left5 left6 middle4 right3"
    cards:
      - type: vertical-stack
        cards:
          - type: custom:mushroom-title-card
            title: |-
              {% set time = now().hour %} {% if time >= 18 %}
                Good Evening!
              {% elif time >= 12 %}
                Good Afternoon!
              {% elif time >= 5 %}
                Good Morning!
              {% else %}
                Welcome!
              {% endif %}
            alignment: start
            subtitle: |
              Today is {{ now().strftime('%a %b %-d - %-I:%M %p') }}
          - type: custom:paper-buttons-row
            styles:
              justify-content: flex-start
              flex-wrap: wrap
              font-size: 1.2em
              line-height: 1.2em
              margin: 6px 0 12px 0
              width: 100%
            buttons:
              - layout: name
                name: Outside is
                styles:
                  button:
                    flex-basis: 1
                    flex-shrink: 0
                  name:
                    color: var(--primary-text-color)
              - layout: name|icon
                entity: weather.klou
                name: >-
                  {{ state_attr('weather.klou', 'temperature') }} °F, {{
                  states('weather.klou') | title }}
                icon: "{{ states('weather.klou') }}"
                tap_action:
                  action: navigate
                  navigation_path: /family-foyer/notifications
                styles:
                  icon:
                    color: var(--md-sys-color-on-primary)
                  name:
                    color: var(--md-sys-color-on-primary)
                    font-weight: bold
                  button:
                    padding: 2px 8px
                    background-color: var(--md-sys-color-primary)
                    border-radius: 24px
                    flex-basis: 1
                    flex-shrink: 0
                    display: flex
              - layout: name
                name: Wind
                styles:
                  button:
                    flex-basis: 1
                    flex-shrink: 0
                  name:
                    color: var(--primary-text-color)
              - layout: name
                entity: weather.klou
                name: >-
                  {% set weather = states.weather.klou.attributes %} {{
                  weather.wind_speed }} {{ weather.wind_speed_unit }}
                tap_action:
                  action: navigate
                  navigation_path: /family-foyer/notifications
                styles:
                  name:
                    color: var(--md-sys-color-on-primary)
                    font-weight: bold
                  button:
                    padding: 2px 8px
                    background-color: var(--md-sys-color-primary)
                    border-radius: 24px
                    flex-basis: 1
                    flex-shrink: 0
                    display: flex
              - layout: name
                name: Humidity
                styles:
                  button:
                    flex-basis: 1
                    flex-shrink: 0
                  name:
                    color: var(--primary-text-color)
              - layout: name
                entity: weather.klou
                name: "{{ state_attr('weather.klou', 'humidity') }} %"
                tap_action:
                  action: navigate
                  navigation_path: /family-foyer/notifications
                styles:
                  name:
                    color: var(--md-sys-color-on-primary)
                    font-weight: bold
                  button:
                    padding: 2px 8px
                    background-color: var(--md-sys-color-primary)
                    border-radius: 24px
                    flex-basis: 1
                    flex-shrink: 0
                    display: flex
        view_layout:
          grid-area: title1
      - type: vertical-stack
        cards:
          - type: custom:button-card
            show_name: false
            show_icon: false
            tap_action:
              action: none
            styles:
              card:
                - background: none
                - box-shadow: none
                - padding: 4px
          - type: custom:simple-tabs
            pre-load: true
            alignment: start
            active-background: var(--md-sys-color-tertiary)
            active-text-color: var(--md-sys-color-on-tertiary)
            tabs:
              - title: Basement
                icon: mdi:hvac
                card:
                  type: markdown
                  content: "## New Tab Content"
              - title: Upstairs
                icon: mdi:hvac
                card:
                  type: custom:vertical-stack-in-card
                  cards:
                    - type: custom:mushroom-template-card
                      entity: switch.right_walkway_water_tap
                      primary: >
                        {{ states('sensor.right_walkway_water_tap_flow') |
                        round(1) }} m³/h
                      secondary: >
                        {% if is_state('switch.right_walkway_water_tap', 'on')
                        %}
                          Running for {{
                            ((as_timestamp(now()) - as_timestamp(states.switch.right_walkway_water_tap.last_changed)) / 60) | int
                          }} minutes
                        {% else %}
                          Water Flow Rate
                        {% endif %}
                      icon: mdi:water
                      tap_action:
                        action: toggle
                      color: >
                        {% set flow =
                        states('sensor.right_walkway_water_tap_flow') | float %}

                        {% if flow < 0.1 %} #A9A9A9

                        {% elif flow < 0.3 %} #ffd34f

                        {% elif flow < 0.6 %} #9fe7e8

                        {% else %} #36a9e2

                        {% endif %}
                      features_position: bottom
                    - type: custom:config-template-card
                      entities: sensor.right_walkway_water_tap_flow
                      variables:
                        - states['sensor.right_walkway_water_tap_flow'].state
                      card:
                        type: custom:mini-graph-card
                        entities:
                          - entity: sensor.right_walkway_water_tap_flow
                            name: Water Flow
                            color: |-
                              ${vars[0] < 0.1 ? '#a9a9a9' :     
                                (vars[0] < 0.3 ? '#ffd34f' :
                                (vars[0] < 0.6 ? '#9fe7e8' : '#36a9e2'))}
                        hours_to_show: 12
                        line_width: 3
                        font_size: 50
                        animate: true
                        show:
                          name: false
                          icon: false
                          state: false
                          legend: false
                          fill: fade
                    - type: custom:mushroom-template-card
                      entity: sensor.current_water_restriction
                      primary: |
                        {{ states('sensor.current_water_restriction') }} 
                      tap_action:
                        action: more-info
                      icon: |
                        {% if is_state('input_boolean.water_level_1', 'on') %}
                          mdi:numeric-1-box-outline
                        {% elif is_state('input_boolean.water_level_2', 'on') %}
                          mdi:numeric-2-box-outline
                        {% elif is_state('input_boolean.water_level_3', 'on') %}
                          mdi:numeric-3-box-outline
                        {% elif is_state('input_boolean.water_level_4', 'on') %}
                          mdi:numeric-4-box-outline
                        {% else %}
                          mdi:water
                        {% endif %}
                      color: |
                        {% if is_state('input_boolean.water_level_1', 'on') %}
                          #DFFF00
                        {% elif is_state('input_boolean.water_level_2', 'on') %}
                          #FFFF00
                        {% elif is_state('input_boolean.water_level_3', 'on') %}
                          #FFA500
                        {% elif is_state('input_boolean.water_level_4', 'on') %}
                          #FF0000
                        {% else %}
                          #89CFF0
                        {% endif %}
                      features_position: bottom
              - title: Downstairs
                icon: mdi:hvac
                card:
                  type: vertical-stack
                  cards:
                    - type: custom:vertical-stack-in-card
                      cards:
                        - type: horizontal-stack
                          cards:
                            - type: custom:mushroom-climate-card
                              entity: climate.thermostat
                        - type: horizontal-stack
                          cards:
                            - type: custom:mushroom-climate-card
                              entity: climate.thermostat
                              primary_info: none
                              secondary_info: none
                              icon_type: none
                              show_temperature_control: true
                            - type: custom:mushroom-climate-card
                              entity: climate.thermostat
                              primary_info: none
                              secondary_info: none
                              icon_type: none
                              hvac_modes:
                                - cool
                                - heat
                                - fan_only
                                - "off"
        view_layout:
          grid-area: title2
      - type: vertical-stack
        cards:
          - type: horizontal-stack
            cards:
              - type: custom:mushroom-title-card
                title: Calendar
                title_tap_action:
                  action: navigate
                  navigation_path: /family-foyer/calendar
              - type: custom:mushroom-chips-card
                chips:
                  - type: conditional
                    conditions:
                      - entity: alarm_control_panel.alarmo
                        state: triggered
                    chip:
                      type: template
                      entity: alarm_control_panel.alarmo
                      content: Triggered
                      tap_action:
                        action: navigate
                        navigation_path: /family-foyer/alarmo
                      icon: mdi:bell-ring
                      icon_color: "#d14f3a"
                  - type: conditional
                    conditions:
                      - entity: alarm_control_panel.alarmo
                        state: armed_away
                    chip:
                      type: template
                      entity: alarm_control_panel.alarmo
                      content: Armed away
                      tap_action:
                        action: navigate
                        navigation_path: /family-foyer/alarmo
                      icon: mdi:shield-lock
                      icon_color: "#69ac5a"
                  - type: conditional
                    conditions:
                      - entity: alarm_control_panel.alarmo
                        state: arming
                    chip:
                      type: template
                      entity: alarm_control_panel.alarmo
                      content: Arming
                      tap_action:
                        action: navigate
                        navigation_path: /family-foyer/alarmo
                      icon: mdi:shield
                      icon_color: "#efa340"
                  - type: conditional
                    conditions:
                      - entity: alarm_control_panel.alarmo
                        state: pending
                    chip:
                      type: template
                      entity: alarm_control_panel.alarmo
                      content: Pending
                      tap_action:
                        action: navigate
                        navigation_path: /family-foyer/alarmo
                      icon: mdi:shield-outline
                      icon_color: "#efa340"
                  - type: conditional
                    conditions:
                      - entity: alarm_control_panel.alarmo
                        state: disarmed
                    chip:
                      type: template
                      entity: alarm_control_panel.alarmo
                      tap_action:
                        action: navigate
                        navigation_path: /family-foyer/alarmo
                      icon: mdi:shield-off
                      icon_color: "#disarmed"
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: sensor.latest_nearby_quake
                        state_not: No quake
                    chip:
                      type: template
                      entity: sensor.latest_nearby_quake
                      tap_action:
                        action: navigate
                        navigation_path: "#earthquake-warning"
                      icon: mdi:earth
                      icon_color: >
                        {% set mag = state_attr('sensor.latest_nearby_quake',
                        'magnitude') | float(0) %} {% if mag >= 6.0 %} #D32F2F
                        {% elif mag >= 5.0 %} #F4511E {% elif mag >= 4.0 %}
                        #FB8C00 {% elif mag >= 3.0 %} #F9A825 {% elif mag >= 2.0
                        %} #FBC02D {% else %} #388E3C {% endif %}
                  - type: conditional
                    conditions:
                      - condition: numeric_state
                        entity: sensor.volcano_rotorua
                        above: 0
                    chip:
                      type: template
                      entity: sensor.volcano_rotorua
                      tap_action:
                        action: navigate
                        navigation_path: /family-foyer/notifications
                      icon: >
                        {{ state_attr('sensor.volcano_rotorua', 'icon') or
                        'mdi:image-filter-hdr' }}
                      icon_color: |
                        {% set level = states('sensor.volcano_rotorua') | int %}
                        {% if level == 5 %} red
                        {% elif level == 4 %} deep-orange
                        {% elif level == 3 %} orange
                        {% elif level == 2 %} amber
                        {% elif level == 1 %} yellow
                        {% else %} green
                        {% endif %}
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: sensor.home_metservice_weather_warnings
                        state_not: unknown
                    chip:
                      type: template
                      entity: sensor.home_metservice_weather_warnings
                      tap_action:
                        action: navigate
                        navigation_path: /family-foyer/notifications
                      icon: mdi:alert
                      icon_color: orange
                  - type: template
                    entity: sensor.active_notification_count
                    icon: mdi:bell
                    tap_action:
                      action: navigate
                      navigation_path: /family-foyer/notifications
                    hold_action:
                      action: none
                    card_mod:
                      style: >
                        ha-card {
                          position: relative;
                        }


                        {% set notifications =
                        states('sensor.active_notification_count') | int %}

                        {% if notifications > 0 %}

                        ha-card:after {
                          content: "{{ notifications }}";
                          position: absolute;
                          top: -3px;
                          right: -3px;
                          background-color: var(--md-sys-color-primary);
                          color: var(--md-sys-color-surface-variant);
                          border-radius: 50%;
                          width: 16px;
                          height: 16px;
                          font-size: 10px;
                          line-height: 16px;
                          text-align: center;
                          display: inline-block;
                          font-weight: bold;
                        } {% endif %}
                alignment: end
                card_mod:
                  style: |
                    ha-card {
                      margin-top: 1.3em
                    }
          - entities:
              - entity: calendar.siddharth_s_calendar
                accent_color: "#ff6a6a"
                show_time: false
                show_location: false
              - entity: calendar.2
                accent_color: "#71c8fe"
            days_to_show: 6
            compact_events_to_show: 6
            background_color: transparent
            vertical_line_width: 5px
            event_spacing: 6px
            first_day_of_week: monday
            show_week_numbers: iso
            week_number_color: var(--md-sys-color-on-primary)
            week_number_background_color: var(--md-sys-color-primary)
            month_separator_width: 1px
            month_separator_color: var(--md-sys-color-tertiary)
            today_indicator: pulse
            today_indicator_position: 10% 50%
            today_indicator_color: var(--md-sys-color-primary)
            weekday_font_size: 12px
            month_font_size: 10px
            show_countdown: true
            show_progress_bar: true
            progress_bar_color: var(--md-sys-color-primary)
            weather:
              position: event
              date:
                show_conditions: true
                show_high_temp: true
                show_low_temp: false
                icon_size: 14px
                font_size: 12px
                color: var(--primary-text-color)
              event:
                show_conditions: true
                show_temp: true
                icon_size: 14px
                font_size: 12px
                color: var(--primary-text-color)
              entity: weather.klou
            tap_action:
              action: navigate
              navigation_path: /family-foyer/calendar
            type: custom:calendar-card-pro
        view_layout:
          grid-area: title4
      - type: conditional
        conditions:
          - condition: and
            conditions:
              - condition: state
                entity: media_player.your_media_here
                state_not: "on"
              - condition: state
                entity: media_player.your_media_here
                state_not: playing
        card:
          type: vertical-stack
          cards:
            - type: custom:mushroom-title-card
              title: Weather Forecast
              subtitle: >-
                {% set forecasts =
                state_attr('sensor.met_no_hourly_forecasts_full_hourly_forecast','forecasts')
                %}{% if forecasts %}{% set next_rain = forecasts |
                selectattr('precipitation','gt',0) | list | first %}{% if
                next_rain %}{% set val = next_rain.precipitation | float %}{% if
                val < 2.5 %}🌦️ Light · {{ val | round(1) }} mm · {{
                as_datetime(next_rain.datetime).strftime('%a, %b %d at %I:%M
                %p') }}{% elif val < 7.6 %}🌧️ Moderate · {{ val | round(1) }}
                mm · {{ as_datetime(next_rain.datetime).strftime('%a, %b %d at
                %I:%M %p') }}{% elif val < 35 %}⛈️ Heavy · {{ val | round(1) }}
                mm · {{ as_datetime(next_rain.datetime).strftime('%a, %b %d at
                %I:%M %p') }}{% else %}⛈️ Very heavy · {{ val | round(1) }} mm ·
                {{ as_datetime(next_rain.datetime).strftime('%a, %b %d at %I:%M
                %p') }}{% endif %}{% else %}🌤️ No rain today{% endif %}{% else
                %}N/A 🌤️{% endif %}
              title_tap_action:
                action: navigate
                navigation_path: /family-foyer/notifications
            - type: custom:clock-weather-card-hui-icons
              entity: weather.klou
              sun_entity: sun.sun
              temperature_sensor: sensor.outdoor_temperature
              humidity_sensor: sensor.outdoor_humidity
              forecast_rows: 5
              locale: en-NZ
              time_pattern: HH:mm
              time_format: 12
              date_pattern: ccc, d.MM.yy
              hide_today_section: true
              hide_forecast_section: false
              show_humidity: false
              hide_clock: true
              hide_date: false
              hourly_forecast: false
              use_browser_time: false
              time_zone: null
              show_decimal: false
        view_layout:
          grid-area: title3
      - type: horizontal-stack
        cards:
          - type: custom:stack-in-card
            mode: horizontal
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:decluttering-card
                    template: room_summary_card
                    variables:
                      - room_name: Garage
                      - temperature_sensor: sensor.garage_temperature
                      - humidity_sensor: sensor.garage_humidity
                      - navigation_path: /family-foyer/garage
                  - type: custom:decluttering-card
                    template: big_icon_toggle_card
                    variables:
                      - entity: switch.light_switch_grg
                      - icon: mdi:garage-variant
              - type: custom:mushroom-chips-card
                chips:
                  - type: template
                    entity: switch.light_switch_grg
                    tap_action:
                      action: toggle
                    icon: mdi:light-recessed
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('switch.light_switch_grg', 'on') %} #D3C890 {% else %} #353535 {% endif %};
                          --color: {% if is_state('switch.light_switch_grg', 'on') %} #5b5326 {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                  - type: template
                    entity: binary_sensor.doors_grg
                    tap_action:
                      action: navigate
                      navigation_path: /family-foyer/garage
                    icon: |
                      {% if is_state(entity, 'on') %}
                        mdi:door-open
                      {% else %}
                        mdi:door-closed
                      {% endif %}
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('binary_sensor.doors_grg', 'on') %} #bc000a {% else %} #353535 {% endif %};
                          --color: {% if is_state('binary_sensor.doors_grg', 'on') %} #ffffff {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                        ha-card:after {
                          content: "{{ '' if (expand(states.binary_sensor.doors_grg) | selectattr('state', 'eq', 'on') | list | count) == 0 else expand(states.binary_sensor.doors_grg) | selectattr('state', 'eq', 'on') | list | count }}";
                          position: absolute;
                          top: -10%;
                          right: -10%;
                          display:  {% if is_state('binary_sensor.doors_grg', 'on') %} flex {% else %} none {% endif %};
                          justify-content: center;
                          align-items: center;
                          width: 14px;
                          height: 14px;
                          font-size: 9px;
                          font-weight: 700;
                          border-radius: 50%;
                          background: {% if is_state('binary_sensor.doors_grg', 'on') %} #ffffff {% else %} var(--primary-text-color) {% endif %};
                          color: #b02e25;
                        }
                card_mod:
                  style: |
                    .chip-container {
                      flex-flow: column wrap-reverse !important;
                      display: flex !important;
                      --chip-spacing: 8px;
                      margin: 12px;
                    }
                    .: |
                    .container {
                      align: end;
                    }
            card_mod:
              style: |
                ha-card {
                  background:
                  {% set temp = states('sensor.garage_temperature') | float %}
                    {% if temp < 13 %}
                      rgba(172, 156, 175, 0.8)
                    {% elif temp < 16 %}          
                      rgba(215, 196, 219, 0.8)
                    {% elif temp < 19 %}
                      rgba(163, 217, 245, 0.8)
                    {% elif temp < 22 %}
                      rgba(205, 227, 219, 0.8)
                    {% elif temp < 24 %}
                      rgba(255, 229, 153, 0.8)
                    {% elif temp < 27 %}
                      rgba(247, 190, 129, 0.8)
                    {% else %}
                      rgba(224, 102, 102, 0.8)
                    {% endif %};        
                  max-width: 100%;
                  min-width: 50%;
                  height: 186px;
                }
          - type: custom:stack-in-card
            mode: horizontal
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:decluttering-card
                    template: room_summary_card
                    variables:
                      - room_name: Living
                      - temperature_sensor: sensor.living_room_temperature
                      - humidity_sensor: sensor.living_area_humidity
                      - navigation_path: /family-foyer/living-area
                  - type: custom:decluttering-card
                    template: big_icon_toggle_card
                    variables:
                      - entity: light.lights_sofa
                      - icon: mdi:sofa
              - type: custom:mushroom-chips-card
                chips:
                  - type: template
                    entity: climate.living_room
                    tap_action:
                      action: toggle
                    icon: |
                      {% if is_state(entity, 'heat_cool') %}
                        mdi:heat-pump
                      {% else %}
                        mdi:heat-pump-outline
                      {% endif %}
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: 
                            {% if is_state('climate.living_room', 'heat_cool') and is_state('climate.heat_pump', 'cool') %} #b5e0f7 
                            {% elif is_state('climate.living_room', 'heat_cool') and is_state('climate.heat_pump', 'heat') %} #fa6f66
                            {% elif is_state('climate.living_room', 'heat_cool') and is_state('climate.heat_pump', 'fan_only') %} #DEDEDE
                            {% elif is_state('climate.living_room', 'heat_cool') and is_state('climate.heat_pump', 'off') %} #FFEA00
                            {% else %} #353535 
                            {% endif %};
                          --color: {% if is_state('climate.living_room', 'heat_cool') %} var(--primary-text-color){% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                  - type: template
                    entity: light.lights_sofa
                    tap_action:
                      action: toggle
                    icon: mdi:light-recessed
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('light.lights_sofa', 'on') %} #D3C890 {% else %} #353535 {% endif %};
                          --color: {% if is_state('light.lights_sofa', 'on') %} #5b5326 {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                  - type: template
                    entity: binary_sensor.doors_la
                    tap_action:
                      action: navigate
                      navigation_path: /family-foyer/living-area
                    icon: |
                      {% if is_state(entity, 'on') %}
                        mdi:door-sliding-open
                      {% else %}
                        mdi:door-sliding
                      {% endif %}          
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('binary_sensor.doors_la', 'on') %} #bc000a {% else %} #353535 {% endif %};
                          --color: {% if is_state('binary_sensor.doors_la', 'on') %} #ffffff {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                        ha-card:after {
                          content: "{{ '' if (expand(states.binary_sensor.doors_la) | selectattr('state', 'eq', 'on') | list | count) == 0 else expand(states.binary_sensor.doors_la) | selectattr('state', 'eq', 'on') | list | count }}";
                          position: absolute;
                          top: -10%;
                          right: -10%;
                          display:  {% if is_state('binary_sensor.doors_la', 'on') %} flex {% else %} none {% endif %};
                          justify-content: center;
                          align-items: center;
                          width: 14px;
                          height: 14px;
                          font-size: 9px;
                          font-weight: 700;
                          border-radius: 50%;
                          background: {% if is_state('binary_sensor.doors_la', 'on') %} #ffffff {% else %} var(--primary-text-color) {% endif %};
                          color: #b02e25;
                        }
                card_mod:
                  style: |
                    .chip-container {
                      flex-flow: column wrap-reverse !important;
                      display: flex !important;
                      --chip-spacing: 8px;
                      margin: 12px;
                    }
                    .: |
                    .container {
                      align: end;
                    }
            card_mod:
              style: |
                ha-card {
                  background:
                  {% set temp = states('sensor.living_room_temperature') | float %}
                    {% if temp < 13 %}
                      rgba(172, 156, 175, 0.8)
                    {% elif temp < 16 %}          
                      rgba(215, 196, 219, 0.8)
                    {% elif temp < 19 %}
                      rgba(163, 217, 245, 0.8)
                    {% elif temp < 22 %}
                      rgba(205, 227, 219, 0.8)
                    {% elif temp < 24 %}
                      rgba(255, 229, 153, 0.8)
                    {% elif temp < 27 %}
                      rgba(247, 190, 129, 0.8)
                    {% else %}
                      rgba(224, 102, 102, 0.8)
                    {% endif %};        
                  max-width: 100%;
                  min-width: 50%;
                  height: 186px;
                }
        view_layout:
          grid-area: left3
      - type: horizontal-stack
        cards:
          - type: custom:stack-in-card
            mode: horizontal
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:decluttering-card
                    template: room_summary_card
                    variables:
                      - room_name: Master
                      - temperature_sensor: sensor.master_bedroom_temperature
                      - humidity_sensor: sensor.master_bedroom_humidity
                      - navigation_path: /family-foyer/master
                  - type: custom:decluttering-card
                    template: big_icon_toggle_card
                    variables:
                      - entity: light.lights_mbr
                      - icon: mdi:bed-king
              - type: custom:mushroom-chips-card
                chips:
                  - type: template
                    entity: climate.master_bedroom
                    tap_action:
                      action: toggle
                    icon: |
                      {% if is_state(entity, 'heat_cool') %}
                        mdi:heat-pump
                      {% else %}
                        mdi:heat-pump-outline
                      {% endif %}
                    card_mod:
                      style: |
                        ha-card {
                        --chip-background: 
                          {% if is_state('climate.master_bedroom', 'heat_cool') and is_state('climate.heat_pump', 'cool') %} #b5e0f7 
                          {% elif is_state('climate.master_bedroom', 'heat_cool') and is_state('climate.heat_pump', 'heat') %} #fa6f66
                          {% elif is_state('climate.master_bedroom', 'heat_cool') and is_state('climate.heat_pump', 'fan_only') %} #DEDEDE
                          {% elif is_state('climate.master_bedroom', 'heat_cool') and is_state('climate.heat_pump', 'off') %} #FFEA00
                          {% else %} #353535 
                          {% endif %};
                          --color: {% if is_state('climate.master_bedroom', 'heat_cool') %} var(--primary-text-color){% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }        
                  - type: template
                    entity: light.lights_mbr
                    tap_action:
                      action: toggle
                    icon: mdi:light-recessed
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('light.lights_mbr', 'on') %} #D3C890 {% else %} #353535 {% endif %};
                          --color: {% if is_state('light.lights_mbr', 'on') %} #5b5326 {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                  - type: template
                    entity: binary_sensor.sliding_door_sensor_mbr_contact
                    tap_action:
                      action: navigate
                      navigation_path: /family-foyer/master
                    icon: |
                      {% if is_state(entity, 'on') %}
                        mdi:door-sliding-open
                      {% else %}
                        mdi:door-sliding
                      {% endif %}
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('binary_sensor.sliding_door_sensor_mbr_contact', 'on') %} #bc000a {% else %} #353535 {% endif %};
                          --color: {% if is_state('binary_sensor.sliding_door_sensor_mbr_contact', 'on') %} #ffffff {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                        ha-card:after {
                          content: "{{ '' if (expand(states.binary_sensor.sliding_door_sensor_mbr_contact) | selectattr('state', 'eq', 'on') | list | count) == 0 else expand(states.binary_sensor.sliding_door_sensor_mbr_contact) | selectattr('state', 'eq', 'on') | list | count }}";
                          position: absolute;
                          top: -10%;
                          right: -10%;
                          display:  {% if is_state('binary_sensor.sliding_door_sensor_mbr_contact', 'on') %} flex {% else %} none {% endif %};
                          justify-content: center;
                          align-items: center;
                          width: 14px;
                          height: 14px;
                          font-size: 9px;
                          font-weight: 700;
                          border-radius: 50%;
                          background: {% if is_state('binary_sensor.sliding_door_sensor_mbr_contact', 'on') %} #ffffff {% else %} var(--primary-text-color) {% endif %};
                          color: #b02e25;
                        }                   
                card_mod:
                  style: |
                    .chip-container {
                      flex-flow: column wrap-reverse !important;
                      display: flex !important;
                      --chip-spacing: 8px;
                      margin: 12px;
                    }
                    .: |
                    .container {
                      align: end;
                    }
            card_mod:
              style: |
                ha-card {
                  background:
                  {% set temp = states('sensor.master_bedroom_temperature') | float %}
                    {% if temp < 13 %}
                      rgba(172, 156, 175, 0.8)
                    {% elif temp < 16 %}          
                      rgba(215, 196, 219, 0.8)
                    {% elif temp < 19 %}
                      rgba(163, 217, 245, 0.8)
                    {% elif temp < 22 %}
                      rgba(205, 227, 219, 0.8)
                    {% elif temp < 24 %}
                      rgba(255, 229, 153, 0.8)
                    {% elif temp < 27 %}
                      rgba(247, 190, 129, 0.8)
                    {% else %}
                      rgba(224, 102, 102, 0.8)
                    {% endif %};        
                  max-width: 100%;
                  min-width: 50%;
                  height: 186px;
                }
          - type: custom:stack-in-card
            mode: horizontal
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:decluttering-card
                    template: room_summary_card
                    variables:
                      - room_name: Baby
                      - temperature_sensor: sensor.baby_room_temperature
                      - humidity_sensor: sensor.baby_room_humidity
                      - navigation_path: /family-foyer/baby
                  - type: custom:decluttering-card
                    template: big_icon_toggle_card
                    variables:
                      - entity: light.lights_br
                      - icon: mdi:baby-carriage
              - type: custom:mushroom-chips-card
                chips:
                  - type: template
                    entity: climate.baby_room
                    tap_action:
                      action: toggle
                    icon: |
                      {% if is_state(entity, 'heat_cool') %}
                        mdi:heat-pump
                      {% else %}
                        mdi:heat-pump-outline
                      {% endif %}
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: 
                            {% if is_state('climate.baby_room', 'heat_cool') and is_state('climate.heat_pump', 'cool') %} #b5e0f7 
                            {% elif is_state('climate.baby_room', 'heat_cool') and is_state('climate.heat_pump', 'heat') %} #fa6f66
                            {% elif is_state('climate.baby_room', 'heat_cool') and is_state('climate.heat_pump', 'fan_only') %} #DEDEDE
                            {% elif is_state('climate.baby_room', 'heat_cool') and is_state('climate.heat_pump', 'off') %} #FFEA00
                            {% else %} #353535 
                            {% endif %};
                          --color: {% if is_state('climate.baby_room', 'heat_cool') %} var(--primary-text-color){% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }    
                  - type: template
                    entity: light.lights_br
                    tap_action:
                      action: toggle
                    icon: mdi:light-recessed
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('light.lights_br', 'on') %} #D3C890 {% else %} #353535 {% endif %};
                          --color: {% if is_state('light.lights_br', 'on') %} #5b5326 {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                  - type: template
                    entity: binary_sensor.door_sensor_br_contact
                    tap_action:
                      action: navigate
                      navigation_path: /family-foyer/baby
                    icon: |
                      {% if is_state(entity, 'on') %}
                        mdi:door-open
                      {% else %}
                        mdi:door
                      {% endif %}
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('binary_sensor.doors_br', 'on') %} #bc000a {% else %} #353535 {% endif %};
                          --color: {% if is_state('binary_sensor.doors_br', 'on') %} #ffffff {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                        ha-card:after {
                          content: "{{ '' if (expand(states.binary_sensor.doors_br) | selectattr('state', 'eq', 'on') | list | count) == 0 else expand(states.binary_sensor.doors_br) | selectattr('state', 'eq', 'on') | list | count }}";
                          position: absolute;
                          top: -10%;
                          right: -10%;
                          display:  {% if is_state('binary_sensor.doors_br', 'on') %} flex {% else %} none {% endif %};
                          justify-content: center;
                          align-items: center;
                          width: 14px;
                          height: 14px;
                          font-size: 9px;
                          font-weight: 700;
                          border-radius: 50%;
                          background: {% if is_state('binary_sensor.doors_br', 'on') %} #ffffff {% else %} var(--primary-text-color) {% endif %};
                          color: #b02e25;
                        }
                card_mod:
                  style: |
                    .chip-container {
                      flex-flow: column wrap-reverse !important;
                      display: flex !important;
                      --chip-spacing: 8px;
                      margin: 12px;
                    }
                    .: |
                    .container {
                      align: end;
                    }
            card_mod:
              style: |
                ha-card {
                  background:
                  {% set temp = states('sensor.baby_room_temperature') | float %}
                    {% if temp < 13 %}
                      rgba(172, 156, 175, 0.8)
                    {% elif temp < 16 %}          
                      rgba(215, 196, 219, 0.8)
                    {% elif temp < 19 %}
                      rgba(163, 217, 245, 0.8)
                    {% elif temp < 22 %}
                      rgba(205, 227, 219, 0.8)
                    {% elif temp < 24 %}
                      rgba(255, 229, 153, 0.8)
                    {% elif temp < 27 %}
                      rgba(247, 190, 129, 0.8)
                    {% else %}
                      rgba(224, 102, 102, 0.8)
                    {% endif %};        
                  max-width: 100%;
                  min-width: 50%;
                  height: 186px;
                }
        view_layout:
          grid-area: left4
      - type: horizontal-stack
        cards:
          - type: custom:stack-in-card
            mode: horizontal
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:decluttering-card
                    template: room_summary_card
                    variables:
                      - room_name: Single
                      - temperature_sensor: sensor.single_guest_room_temperature
                      - humidity_sensor: sensor.single_guest_room_humidity
                      - navigation_path: /family-foyer/single-guest
                  - type: custom:decluttering-card
                    template: big_icon_toggle_card
                    variables:
                      - entity: light.lights_sgr
                      - icon: mdi:account-plus
              - type: custom:mushroom-chips-card
                chips:
                  - type: template
                    entity: climate.single_guest_room
                    tap_action:
                      action: toggle
                    icon: |
                      {% if is_state(entity, 'heat_cool') %}
                        mdi:heat-pump
                      {% else %}
                        mdi:heat-pump-outline
                      {% endif %}
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: 
                            {% if is_state('climate.single_guest_room', 'heat_cool') and is_state('climate.heat_pump', 'cool') %} #b5e0f7 
                            {% elif is_state('climate.single_guest_room', 'heat_cool') and is_state('climate.heat_pump', 'heat') %} #fa6f66 
                            {% elif is_state('climate.single_guest_room', 'heat_cool') and is_state('climate.heat_pump', 'fan_only') %} #DEDEDE
                                            {% elif is_state('climate.single_guest_room', 'heat_cool') and is_state('climate.heat_pump', 'off') %} #FFEA00
                            {% else %} #353535 
                            {% endif %};
                          --color: {% if is_state('climate.single_guest_room', 'heat_cool') %} var(--primary-text-color){% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }    
                  - type: template
                    entity: light.lights_sgr
                    tap_action:
                      action: toggle
                    icon: mdi:light-recessed
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('light.lights_sgr', 'on') %} #D3C890 {% else %} #353535 {% endif %};
                          --color: {% if is_state('light.lights_sgr', 'on') %} #5b5326 {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                  - type: template
                    entity: binary_sensor.doors_sgr
                    tap_action:
                      action: navigate
                      navigation_path: /mobile-dashboard/single-guest
                    icon: |
                      {% if is_state(entity, 'on') %}
                        mdi:door-open
                      {% else %}
                        mdi:door-closed
                      {% endif %}
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('binary_sensor.doors_sgr', 'on') %} #bc000a {% else %} #353535 {% endif %};
                          --color: {% if is_state('binary_sensor.doors_sgr', 'on') %} #ffffff {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                        ha-card:after {
                          content: "{{ '' if (expand(states.binary_sensor.doors_sgr) | selectattr('state', 'eq', 'on') | list | count) == 0 else expand(states.binary_sensor.doors_sgr) | selectattr('state', 'eq', 'on') | list | count }}";
                          position: absolute;
                          top: -10%;
                          right: -10%;
                          display:  {% if is_state('binary_sensor.doors_sgr', 'on') %} flex {% else %} none {% endif %};
                          justify-content: center;
                          align-items: center;
                          width: 14px;
                          height: 14px;
                          font-size: 9px;
                          font-weight: 700;
                          border-radius: 50%;
                          background: {% if is_state('binary_sensor.doors_sgr', 'on') %} #ffffff {% else %} var(--primary-text-color) {% endif %};
                          color: #b02e25;
                        }                      
                card_mod:
                  style: |
                    .chip-container {
                      flex-flow: column wrap-reverse !important;
                      display: flex !important;
                      --chip-spacing: 8px;
                      margin: 12px;
                    }
                    .: |
                    .container {
                      align: end;
                    }
            card_mod:
              style: |
                ha-card {
                  background:
                  {% set temp = states('sensor.single_guest_room_temperature') | float %}
                    {% if temp < 13 %}
                      rgba(172, 156, 175, 0.8)
                    {% elif temp < 16 %}          
                      rgba(215, 196, 219, 0.8)
                    {% elif temp < 19 %}
                      rgba(163, 217, 245, 0.8)
                    {% elif temp < 22 %}
                      rgba(205, 227, 219, 0.8)
                    {% elif temp < 24 %}
                      rgba(255, 229, 153, 0.8)
                    {% elif temp < 27 %}
                      rgba(247, 190, 129, 0.8)
                    {% else %}
                      rgba(224, 102, 102, 0.8)
                    {% endif %};        
                  max-width: 100%;
                  min-width: 50%;
                  height: 186px;
                }
          - type: custom:stack-in-card
            mode: horizontal
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:decluttering-card
                    template: room_summary_card
                    variables:
                      - room_name: Multiple
                      - temperature_sensor: sensor.multiple_guest_room_temperature
                      - humidity_sensor: sensor.multiple_guest_room_humidity
                      - navigation_path: /family-foyer/multiple-guest
                  - type: custom:decluttering-card
                    template: big_icon_toggle_card
                    variables:
                      - entity: light.lights_mgr
                      - icon: mdi:account-group
              - type: custom:mushroom-chips-card
                chips:
                  - type: template
                    entity: climate.multiple_guest_room
                    tap_action:
                      action: toggle
                    icon: |
                      {% if is_state(entity, 'heat_cool') %}
                        mdi:heat-pump
                      {% else %}
                        mdi:heat-pump-outline
                      {% endif %}
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: 
                            {% if is_state('climate.multiple_guest_room', 'heat_cool') and is_state('climate.heat_pump', 'cool') %} #b5e0f7 
                            {% elif is_state('climate.multiple_guest_room', 'heat_cool') and is_state('climate.heat_pump', 'heat') %} #fa6f66 
                            {% elif is_state('climate.multiple_guest_room', 'heat_cool') and is_state('climate.heat_pump', 'fan_only') %} #DEDEDE
                            {% elif is_state('climate.multiple_guest_room', 'heat_cool') and is_state('climate.heat_pump', 'off') %} #FFEA00
                            {% else %} #353535 
                            {% endif %};
                          --color: {% if is_state('climate.multiple_guest_room', 'heat_cool') %} var(--primary-text-color){% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }    
                  - type: template
                    entity: light.lights_mgr
                    tap_action:
                      action: toggle
                    icon: mdi:light-recessed
                    card_mod:
                      style: |
                        ha-card {
                          --chip-background: {% if is_state('light.lights_mgr', 'on') %} #D3C890 {% else %} #353535 {% endif %};
                          --color: {% if is_state('light.lights_mgr', 'on') %} #5b5326 {% else %} #dbdbdb {% endif %};
                          justify-content: center;
                          height: 35px !important;
                          width: 35px !important;
                          border: 0 !important;
                          box-shadow: none !important;
                          border-radius: 22px !important;
                        }
                card_mod:
                  style: |
                    .chip-container {
                      flex-flow: column wrap-reverse !important;
                      display: flex !important;
                      --chip-spacing: 8px;
                      margin: 12px;
                    }
                    .: |
                    .container {
                      align: end;
                    }
            card_mod:
              style: |
                ha-card {
                  background:
                  {% set temp = states('sensor.multiple_guest_room_temperature') | float %}
                    {% if temp < 13 %}
                      rgba(172, 156, 175, 0.8)
                    {% elif temp < 16 %}          
                      rgba(215, 196, 219, 0.8)
                    {% elif temp < 19 %}
                      rgba(163, 217, 245, 0.8)
                    {% elif temp < 22 %}
                      rgba(205, 227, 219, 0.8)
                    {% elif temp < 24 %}
                      rgba(255, 229, 153, 0.8)
                    {% elif temp < 27 %}
                      rgba(247, 190, 129, 0.8)
                    {% else %}
                      rgba(224, 102, 102, 0.8)
                    {% endif %};        
                  max-width: 100%;
                  min-width: 50%;
                  height: 186px;
                }
        view_layout:
          grid-area: middle3
      - type: vertical-stack
        cards:
          - type: custom:advanced-camera-card
            cameras:
              - camera_entity: camera.driveway_low_resolution_channel
            card_mod:
              style: |
                ha-card {
                  height: 150px; /* set to whatever height you want */
                  overflow: hidden;
                }
          - type: custom:mushroom-chips-card
            alignment: start
            chips:
              - type: template
                entity: switch.driveway_lights
                icon: mdi:outdoor-lamp
                tap_action:
                  action: toggle
                content: Driveway Light
                card_mod:
                  style:
                    .: |
                      ha-card {
                        --text-color: {{ 'var(--primary-background-color)' if is_state('switch.driveway_lights', 'on') else 'none' }};
                        --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.driveway_lights', 'on') else 'none' }};
                        --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.driveway_lights', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                      }
              - type: template
                entity: switch.porch_lights
                icon: mdi:string-lights
                tap_action:
                  action: toggle
                content: Porch Light
                card_mod:
                  style:
                    .: |
                      ha-card {
                        --text-color: {{ 'var(--primary-background-color)' if is_state('switch.porch_lights', 'on') else 'none' }};
                        --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.porch_lights', 'on') else 'none' }};
                        --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.porch_lights', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                      }
        view_layout:
          grid-area: left5
      - type: vertical-stack
        cards:
          - type: custom:advanced-camera-card
            cameras:
              - camera_entity: camera.baby_cam_medium_resolution_channel
            card_mod:
              style: |
                ha-card {
                  height: 150px; /* set to whatever height you want */
                  overflow: hidden;
                }
          - type: custom:mushroom-chips-card
            alignment: start
            chips:
              - type: template
                entity: switch.master_bedroom_dresser_light_switch
                icon: mdi:lamp
                tap_action:
                  action: toggle
                card_mod:
                  style:
                    .: |
                      ha-card {
                        --text-color: {{ 'var(--primary-background-color)' if is_state('switch.master_bedroom_dresser_light_switch', 'on') else 'none' }};
                        --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.master_bedroom_dresser_light_switch', 'on') else 'none' }};
                        --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.master_bedroom_dresser_light_switch', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                      }  
                content: Dresser Light
        view_layout:
          grid-area: left6
      - type: vertical-stack
        cards:
          - type: custom:advanced-camera-card
            cameras:
              - camera_entity: camera.garage_low_resolution_channel
            card_mod:
              style: |
                ha-card {
                  height: 150px; /* set to whatever height you want */
                  overflow: hidden;
                }
          - type: custom:mushroom-chips-card
            alignment: start
            chips:
              - type: template
                entity: switch.light_switch_grg
                icon: mdi:light-recessed
                tap_action:
                  action: toggle
                content: Garage Light
                card_mod:
                  style:
                    .: |
                      ha-card {
                        --text-color: {{ 'var(--primary-background-color)' if is_state('switch.light_switch_grg', 'on') else 'none' }};
                        --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.light_switch_grg', 'on') else 'none' }};
                        --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.light_switch_grg', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                      }   
              - type: template
                entity: switch.rear_light_switch_grg
                icon: mdi:outdoor-lamp
                tap_action:
                  action: toggle
                content: Rear Garage Light
                card_mod:
                  style:
                    .: |
                      ha-card {
                        --text-color: {{ 'var(--primary-background-color)' if is_state('switch.rear_light_switch_grg', 'on') else 'none' }};
                        --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.rear_light_switch_grg', 'on') else 'none' }};
                        --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.rear_light_switch_grg', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                      }
        view_layout:
          grid-area: middle4
      - type: vertical-stack
        cards:
          - type: custom:advanced-camera-card
            cameras:
              - camera_entity: camera.backyard_medium_resolution_channel
            card_mod:
              style: |
                ha-card {
                  height: 150px; /* set to whatever height you want */
                  overflow: hidden;
                }
          - type: custom:mushroom-chips-card
            alignment: start
            chips:
              - type: template
                entity: switch.patio_main_switch_la
                icon: mdi:light-recessed
                tap_action:
                  action: toggle
                content: Main Patio Light
                card_mod:
                  style:
                    .: |
                      ha-card {
                        --text-color: {{ 'var(--primary-background-color)' if is_state('switch.patio_main_switch_la', 'on') else 'none' }};
                        --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.patio_main_switch_la', 'on') else 'none' }};
                        --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.patio_main_switch_la', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                      }
        view_layout:
          grid-area: right3
      - type: custom:mushroom-title-card
        title: Driveway
        title_tap_action:
          action: navigate
          navigation_path: /family-foyer/camera
        card_mod:
          style: |
            ha-card {
              --title-font-size: 20px !important;
              --layout-padding: 0px;
            }  
        view_layout:
          grid-area: title5
      - type: custom:mushroom-title-card
        title: BabyCam
        title_tap_action:
          action: navigate
          navigation_path: /family-foyer/camera
        view_layout:
          grid-area: title6
        card_mod:
          style: |
            ha-card {
              --title-font-size: 20px !important;
            }  
      - type: custom:mushroom-title-card
        title: Garage
        title_tap_action:
          action: navigate
          navigation_path: /family-foyer/camera
        card_mod:
          style: |
            ha-card {
              --title-font-size: 20px !important;
            }  
        view_layout:
          grid-area: title7
      - type: custom:mushroom-title-card
        title: Backyard
        title_tap_action:
          action: navigate
          navigation_path: /family-foyer/camera
        card_mod:
          style: |
            ha-card {
              --title-font-size: 20px !important;
            }  
        view_layout:
          grid-area: title8
    icon: mdi:home-outline
    path: overview
    background:
      opacity: 80
      alignment: center
      size: cover
      repeat: repeat
      attachment: fixed
      image: /api/image/serve/fcc922c73fe69b2ee6bc6fc24d7ebe44/original
  - type: sections
    max_columns: 1
    icon: mdi:shield-lock
    path: alarmo
    title: Alarmo
    sections:
      - type: grid
        cards:
          - type: custom:mushroom-template-card
            features_position: bottom
            primary: Camera
            icon: mdi:cctv
            color: var(--primary-text-color)
            secondary: Navigate to Camera View
            tap_action:
              action: navigate
              navigation_path: /family-foyer/camera
            card_mod:
              style: |
                ha-card {
                  background: #555555;
                }
          - states:
              - arm_away
              - arm_night
            type: alarm-panel
            entity: alarm_control_panel.alarmo
            theme: Material You
    badges:
      - type: custom:mushroom-chips-card
        chips:
          - type: template
            tap_action:
              action: navigate
              navigation_path: /family-foyer/overview
            icon: mdi:arrow-left
            card_mod:
              style:
                .: |
                  ha-card {
                    --color: {{ 'var(--md-sys-color-on-primary)'  }};
                    --chip-background: {{ 'var(--md-sys-color-primary)' }};
                    margin-top: 0.7em;
                  }
    header:
      layout: responsive
      badges_position: top
      badges_wrap: wrap
    cards: []
    background:
      opacity: 90
      alignment: center
      size: cover
      repeat: repeat
      attachment: fixed
      image: /api/image/serve/fcc922c73fe69b2ee6bc6fc24d7ebe44/original
    subview: true
  - type: sections
    max_columns: 3
    title: Notifications
    path: notifications
    icon: mdi:bell-outline
    sections:
      - type: grid
        cards:
          - type: vertical-stack
            cards:
              - type: custom:decluttering-card
                template: mushroom_title_card
                variables:
                  - title: Notifications
              - type: custom:mushroom-chips-card
                chips:
                  - type: conditional
                    conditions:
                      - condition: numeric_state
                        entity: sensor.soil_sensor_corner_moisture
                        below: 40
                    chip:
                      type: template
                      entity: sensor.soil_sensor_corner_moisture
                      icon: mdi:water-percent
                      content: "{{ states('sensor.soil_sensor_corner_moisture') }}%"
                      icon_color: >
                        {% set moisture =
                        states('sensor.soil_sensor_corner_moisture') | int(0) %}
                        {% if moisture >= 40 %}
                          green
                        {% elif moisture >= 25 %}
                          yellow
                        {% else %}
                          red
                        {% endif %}
                      tap_action:
                        action: more-info
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: sensor.house_maintenance_chip_card
                        state: "true"
                    chip:
                      type: template
                      entity: sensor.house_maintenance_chip_card
                      icon: |
                        {% if is_state('input_boolean.rubbish', 'on') %}
                          mdi:trash-can
                        {% elif is_state('input_boolean.recycle', 'on') %}
                          mdi:recycle
                        {% else %} {% endif %}
                      icon_color: |
                        {% if is_state('input_boolean.rubbish', 'on') %}
                          #C41E3A
                        {% elif is_state('input_boolean.recycle', 'on') %}
                          #50C878
                        {% else %} {% endif %}
                      content: |
                        {% if is_state('input_boolean.rubbish', 'on') %}
                          Rubbish Day
                        {% elif is_state('input_boolean.recycle', 'on') %}
                          Recycle Day
                        {% else %} {% endif %}      
                      tap_action:
                        action: more-info
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: binary_sensor.door_sensor_grg_contact
                        state: "on"
                    chip:
                      type: template
                      entity: binary_sensor.door_sensor_grg_contact
                      icon: mdi:garage-variant
                      icon_color: red
                      content: >-
                        {{ 'Garage Opened' if
                        states('binary_sensor.door_sensor_grg_contact') == 'on'
                        else '' }}
                      tap_action:
                        action: navigate
                        navigation_path: /family-foyer/camera
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: binary_sensor.all_doors
                        state: "on"
                    chip:
                      type: template
                      entity: binary_sensor.all_doors
                      content: |
                        {{ expand('binary_sensor.all_doors')
                            | rejectattr('entity_id', 'eq', 'binary_sensor.door_sensor_mbr_contact')
                            | selectattr('state', 'eq', 'on') 
                            | map(attribute='name') 
                            | join(', ') }}
                      icon: |
                        {% set open_doors = expand('binary_sensor.all_doors')
                            | rejectattr('entity_id', 'eq', 'binary_sensor.door_sensor_mbr_contact')
                            | selectattr('state', 'eq', 'on')
                            | list | count %}
                        {{ 'mdi:door-open' if open_doors > 0 else '' }}
                      icon_color: red
                      tap_action:
                        action: more-info
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: sensor.laundry_machine_state
                        state: Delay Run
                    chip:
                      type: entity
                      entity: sensor.laundry_delay_time_remaining
                      icon_color: blue
                      tap_action:
                        action: more-info
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: sensor.laundry_machine_state
                        state: Run
                    chip:
                      type: entity
                      entity: sensor.laundry_time_remaining
                      icon: mdi:washing-machine
                      icon_color: blue
                      tap_action:
                        action: more-info
                  - type: conditional
                    conditions:
                      - condition: numeric_state
                        entity: sensor.dryer_switch_lr_power
                        above: 10
                    chip:
                      type: template
                      entity: sensor.dryer_switch_lr_power
                      tap_action:
                        action: more-info
                      icon: mdi:tumble-dryer
                      icon_color: "#880808"
                      content: Dryer Running
                  - type: conditional
                    conditions:
                      - condition: or
                        conditions:
                          - condition: state
                            entity: media_player.all_speakers
                            state: playing
                          - condition: state
                            entity: media_player.all_speakers
                            state: "on"
                    chip:
                      type: template
                      entity: media_player.all_speakers
                      content: |
                        {{ expand(states.media_player.all_speakers)
                            | selectattr('state', 'eq', 'playing')
                            | list | count }}
                      icon: mdi:music
                      icon_color: var(--primary-text-color)
                      tap_action:
                        action: more-info
              - type: conditional
                conditions:
                  - condition: or
                    conditions:
                      - condition: state
                        entity: input_boolean.rubbish
                        state: "on"
                      - condition: state
                        entity: input_boolean.recycle
                        state: "on"
                card:
                  type: conditional
                  conditions:
                    - condition: or
                      conditions:
                        - condition: state
                          entity: input_boolean.rubbish
                          state: "on"
                        - condition: state
                          entity: input_boolean.recycle
                          state: "on"
                  card:
                    type: vertical-stack
                    cards:
                      - type: custom:webrtc-camera
                        streams:
                          - entity: camera.right_driveway_camera_fluent
                            mode: webrtc
                            media: video,audio,microphone
                            muted: true
                      - type: custom:mushroom-chips-card
                        alignment: start
                        chips:
                          - type: template
                            entity: switch.light_switch_drw
                            icon: mdi:light-recessed
                            tap_action:
                              action: toggle
                            content: Driveway Light
                            card_mod:
                              style:
                                .: |
                                  ha-card {
                                    --text-color: {{ 'var(--primary-background-color)' if is_state('switch.light_switch_drw', 'on') else 'none' }};
                                    --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.light_switch_drw', 'on') else 'none' }};
                                    --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.light_switch_drw', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                                  }
                          - type: template
                            entity: switch.porch_light_drw
                            icon: mdi:light-recessed
                            tap_action:
                              action: toggle
                            content: Porch Light
                            card_mod:
                              style:
                                .: |
                                  ha-card {
                                    --text-color: {{ 'var(--primary-background-color)' if is_state('switch.porch_light_drw', 'on') else 'none' }};
                                    --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.porch_light_drw', 'on') else 'none' }};
                                    --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.porch_light_drw', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                                  }
              - type: grid
                columns: 3
                square: false
                cards:
                  - type: conditional
                    conditions:
                      - condition: or
                        conditions:
                          - condition: state
                            entity: binary_sensor.show_wash_cuddly_timer
                            state: "on"
                          - condition: state
                            entity: timer.wash_cuddly
                            state: idle
                    card:
                      type: custom:timer-bar-card
                      entity: timer.wash_cuddly
                      invert: true
                      bar_direction: rtl
                      bar_width: 60%
                      mushroom:
                        layout: vertical
                        color: "#f385bc"
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: timer.wash_duvet
                        state: idle
                    card:
                      type: custom:timer-bar-card
                      entity: timer.wash_duvet
                      invert: true
                      bar_direction: rtl
                      bar_width: 60%
                      mushroom:
                        layout: vertical
                        color: "#f3c843"
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: timer.heated_towel_rails
                        state: active
                    card:
                      type: custom:timer-bar-card
                      entity: timer.heated_towel_rails
                      invert: true
                      bar_direction: rtl
                      bar_width: 60%
                      mushroom:
                        layout: vertical
                        color: orange
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: timer.exhaust_fans
                        state: active
                    card:
                      type: custom:timer-bar-card
                      entity: timer.exhaust_fans
                      invert: true
                      bar_direction: rtl
                      bar_width: 60%
                      mushroom:
                        layout: vertical
                        color: "#b5e0f7"
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: timer.rice_cooker
                        state: active
                    card:
                      type: custom:timer-bar-card
                      entity: timer.rice_cooker
                      invert: true
                      bar_direction: rtl
                      bar_width: 60%
                      mushroom:
                        layout: vertical
                        color: "#D70040"
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: timer.blender
                        state: active
                    card:
                      type: custom:timer-bar-card
                      entity: timer.blender
                      invert: true
                      bar_direction: rtl
                      bar_width: 60%
                      mushroom:
                        layout: vertical
                        color: "#A020F0"
                  - type: conditional
                    conditions:
                      - condition: state
                        entity: timer.sprinkler
                        state: active
                    card:
                      type: custom:timer-bar-card
                      entity: timer.sprinkler
                      invert: true
                      bar_direction: rtl
                      bar_width: 60%
                      mushroom:
                        layout: vertical
                        color: "#4CBB17"
          - type: vertical-stack
            cards:
              - type: conditional
                conditions:
                  - condition: state
                    entity: light.all_lights
                    state: "on"
                card:
                  type: custom:decluttering-card
                  template: mushroom_title_card
                  variables:
                    - title: Lights
              - type: custom:auto-entities
                card:
                  square: false
                  type: grid
                  columns: 2
                card_param: cards
                filter:
                  include:
                    - group: light.all_lights
                      entity_id: light.lights_*
                      state: "on"
                      options:
                        type: vertical-stack
                        cards:
                          - type: custom:button-card
                            entity: this.entity_id
                            template: light_on_button
                          - type: custom:decluttering-card
                            template: light-on-percentage-button
                            variables:
                              - entity: this.entity_id
                    - group: light.all_lights
                      entity_id: "*led*"
                      state: "on"
                      options:
                        type: vertical-stack
                        cards:
                          - type: custom:button-card
                            entity: this.entity_id
                            template: light_on_button
                          - type: custom:decluttering-card
                            template: light-on-percentage-button
                            variables:
                              - entity: this.entity_id
                  exclude: []
                show_empty: false
                sort:
                  method: entity_id
          - type: vertical-stack
            cards:
              - type: conditional
                conditions:
                  - condition: state
                    entity: switch.all_light_switches
                    state: "on"
                card:
                  type: custom:decluttering-card
                  template: mushroom_title_card
                  variables:
                    - title: Light Switches
              - type: custom:auto-entities
                card:
                  square: false
                  type: grid
                  columns: 2
                card_param: cards
                filter:
                  include:
                    - group: switch.all_light_switches
                      entity_id: "*switch*"
                      state: "on"
                      options:
                        type: custom:button-card
                        entity: this.entity_id
                        icon: mdi:light-recessed
                        template: standard_button
                  exclude: []
                show_empty: false
                sort:
                  method: entity_id
      - type: grid
        cards:
          - type: conditional
            conditions:
              - condition: or
                conditions:
                  - condition: state
                    entity: sensor.home_metservice_weather_warnings
                    state: unavailable
                  - condition: state
                    entity: sensor.home_metservice_weather_warnings
                    state: unknown
            card:
              type: vertical-stack
              cards:
                - type: custom:decluttering-card
                  template: mushroom_title_subtitle_card
                  variables:
                    - title: Description
                    - subtitle: >-
                        {% set aqi =
                        states('sensor.morrinsville_college_new_zealand_air_quality_index')
                        %}{% set dominant_pollutant =
                        states('sensor.morrinsville_college_new_zealand_dominant_pollutant')
                        %}{% if aqi not in ['unknown','unavailable','none','']
                        %}{% set aqi = aqi | int %}{% set aqi_level = ('Good' if
                        aqi <= 50 else 'Moderate' if aqi <= 100 else 'Unhealthy
                        for sensitive groups' if aqi <= 150 else 'Unhealthy' if
                        aqi <= 200 else 'Very Unhealthy' if aqi <= 300 else
                        'Hazardous') %}🌿 Air quality: {{ aqi_level }} (AQI: {{
                        aqi }}) 😶‍🌫️ Pollutant: {{ dominant_pollutant }}{%
                        else %}🌿 Air quality data is currently unavailable.{%
                        endif %}
                - type: markdown
                  content: >
                    {{ states('sensor.home_weather_description') }} {{
                    states('sensor.home_pollen_type') }}
          - type: conditional
            conditions:
              - condition: and
                conditions:
                  - condition: state
                    entity: sensor.home_metservice_weather_warnings
                    state_not: unavailable
                  - condition: state
                    entity: sensor.home_metservice_weather_warnings
                    state_not: unknown
            card:
              type: vertical-stack
              cards:
                - type: custom:mushroom-title-card
                  title: Weather Warning
                  card_mod:
                    style: |
                      ha-card {
                        --title-font-size: 20px !important;
                      }
                - type: markdown
                  content: |2-

                            {{ state_attr('sensor.home_metservice_weather_warnings', 'warnings') | default('No warnings') }}

                      _Issued: {{
                          states.sensor.home_metservice_weather_warnings.last_changed.strftime('%I:%M%p
                          %A, %d %b') }}_
          - type: custom:simple-swipe-card
            auto_hide_pagination: 0
            cards:
              - type: vertical-stack
                cards:
                  - type: custom:decluttering-card
                    template: mushroom_title_subtitle_card
                    variables:
                      - title: Weather Forecast
                      - subtitle: >-
                          {% set forecasts =
                          state_attr('sensor.met_no_hourly_forecasts_full_hourly_forecast','forecasts')
                          %}{% if forecasts %}{% set next_rain = forecasts |
                          selectattr('precipitation','gt',0) | list | first %}{%
                          if next_rain %}{% set val = next_rain.precipitation |
                          float %}{% if val < 2.5 %}🌦️ Light · {{ val |
                          round(1) }} mm · {{
                          as_datetime(next_rain.datetime).strftime('%a, %b %d at
                          %I:%M %p') }}{% elif val < 7.6 %}🌧️ Moderate · {{ val
                          | round(1) }} mm · {{
                          as_datetime(next_rain.datetime).strftime('%a, %b %d at
                          %I:%M %p') }}{% elif val < 35 %}⛈️ Heavy · {{ val |
                          round(1) }} mm · {{
                          as_datetime(next_rain.datetime).strftime('%a, %b %d at
                          %I:%M %p') }}{% else %}⛈️ Very heavy · {{ val |
                          round(1) }} mm · {{
                          as_datetime(next_rain.datetime).strftime('%a, %b %d at
                          %I:%M %p') }}{% endif %}{% else %}🌤️ No rain today{%
                          endif %}{% else %}N/A 🌤️{% endif %}
                  - type: custom:clock-weather-card-hui-icons
                    entity: weather.klou
                    sun_entity: sun.sun
                    temperature_sensor: sensor.outdoor_temperature
                    humidity_sensor: sensor.outdoor_humidity
                    forecast_rows: 5
                    locale: en-NZ
                    time_pattern: HH:mm
                    time_format: 12
                    date_pattern: ccc, d.MM.yy
                    hide_today_section: false
                    hide_forecast_section: false
                    show_humidity: false
                    hide_clock: false
                    hide_date: false
                    hourly_forecast: false
                    use_browser_time: false
                    time_zone: null
                    show_decimal: false
                  - type: custom:button-card
                    color_type: blank-card
              - type: vertical-stack
                cards:
                  - type: custom:decluttering-card
                    template: mushroom_title_subtitle_card
                    variables:
                      - title: Rainfall & Temperature Forecast
                      - subtitle: >-
                          {% set forecasts =
                          state_attr('sensor.met_no_hourly_forecasts_full_hourly_forecast','forecasts')
                          %}{% if forecasts %}{% set values = forecasts |
                          map(attribute='precipitation') | map('float') | list
                          %}{% set total = values | sum %}{% set peak = values |
                          max %}{% set peak_entry = forecasts |
                          selectattr('precipitation','eq', peak) | list | first
                          %}{% if total == 0 %}🌤️ Rainfall: None expected in
                          the next 24 hours{% else %}{% if total < 2.5 %}{% set
                          category = 'Light' %}{% set emoji = '🌦️' %}{% elif
                          total < 7.6 %}{% set category = 'Moderate' %}{% set
                          emoji = '🌧️' %}{% elif total < 35 %}{% set category =
                          'Heavy' %}{% set emoji = '⛈️' %}{% else %}{% set
                          category = 'Very heavy' %}{% set emoji = '🌊' %}{%
                          endif %}{{ emoji }} Rainfall: {{ total | round(1) }}
                          mm ({{ category }}), peaking {{ peak | round(1) }}
                          mm/hr on {{
                          as_datetime(peak_entry.datetime).strftime('%a %I:%M
                          %p') }}.{% endif %}{% else %}🌤️ Rainfall: N/A{% endif
                          %}
                  - type: custom:apexcharts-card
                    header:
                      show: true
                    graph_span: 24h
                    span:
                      start: hour
                    now:
                      show: true
                    apex_config:
                      chart:
                        height: 235
                      legend:
                        show: false
                      grid:
                        yaxis:
                          lines:
                            show: false
                      xaxis:
                        crosshairs:
                          show: false
                        tooltip:
                          enabled: false
                      yaxis:
                        - id: rain
                          title:
                            text: Rain (mm)
                          opposite: false
                        - id: temp
                          title:
                            text: Temp (°C)
                          opposite: true
                          decimalsInFloat: 0
                    series:
                      - entity: sensor.met_no_hourly_forecasts_full_hourly_forecast
                        name: Rain
                        type: column
                        yaxis_id: rain
                        data_generator: |
                          return entity.attributes.forecasts.map(f => {
                            let val = f.precipitation;
                            let color = '#9e9e9e';
                            if (val > 0 && val < 2.5) {
                              color = '#4FC3F7';
                            } else if (val < 7.6) {
                              color = '#0288D1';
                            } else if (val < 35) {
                              color = '#01579B';
                            } else {
                              color = '#311B92';
                            }
                            return {
                              x: new Date(f.datetime).getTime(),
                              y: val,
                              fillColor: color
                            };
                          });
                      - entity: sensor.met_no_hourly_forecasts_full_hourly_forecast
                        name: Temp
                        type: line
                        curve: smooth
                        yaxis_id: temp
                        stroke_width: 2
                        color: "#E53935"
                        data_generator: |
                          return entity.attributes.forecasts.map(f => {
                            return {
                              x: new Date(f.datetime).getTime(),
                              y: f.temperature
                            };
                          });
              - type: vertical-stack
                cards:
                  - type: custom:decluttering-card
                    template: mushroom_title_subtitle_card
                    variables:
                      - title: Wind Speed & Direction Forecast
                      - subtitle: >-
                          {% set forecasts =
                          state_attr('sensor.met_no_hourly_forecasts_full_hourly_forecast','forecasts')
                          %}{% if forecasts %}{% set speeds = forecasts |
                          map(attribute='wind_speed') | map('float') | list %}{%
                          set peak = speeds | max %}{% set peak_entry =
                          forecasts | selectattr('wind_speed','eq', peak) | list
                          | first %}{% set avg_bearing = (forecasts |
                          map(attribute='wind_bearing') | list | sum / forecasts
                          | length) %}{% set direction =
                          ['N','NNE','NE','ENE','E','ESE','SE','SSE','S','SSW','SW','WSW','W','WNW','NW','NNW'][(avg_bearing
                          / 22.5) | round(0) % 16] %}{% if peak < 2 %}{% set
                          category = 'Calm' %}{% set emoji = '🌫️' %}{% elif
                          peak < 6 %}{% set category = 'Light breeze' %}{% set
                          emoji = '🍃' %}{% elif peak < 12 %}{% set category =
                          'Gentle breeze' %}{% set emoji = '🌿' %}{% elif peak <
                          20 %}{% set category = 'Moderate breeze' %}{% set
                          emoji = '🌬️' %}{% elif peak < 29 %}{% set category =
                          'Fresh breeze' %}{% set emoji = '💨' %}{% elif peak <
                          39 %}{% set category = 'Strong breeze' %}{% set emoji
                          = '💨' %}{% elif peak < 50 %}{% set category = 'Near
                          gale' %}{% set emoji = '🌪️' %}{% elif peak < 62 %}{%
                          set category = 'Gale' %}{% set emoji = '🌪️' %}{% elif
                          peak < 75 %}{% set category = 'Strong gale' %}{% set
                          emoji = '🌪️' %}{% else %}{% set category = 'Storm'
                          %}{% set emoji = '🌀' %}{% endif %}{{ emoji }} Winds:
                          {{ peak | round(1) }} km/h ({{ category }}), mostly
                          from {{ direction }}, peaking {{
                          as_datetime(peak_entry.datetime).strftime('%a %I:%M
                          %p') }}.{% else %}🌤️ Winds: N/A{% endif %}
                  - type: custom:apexcharts-card
                    graph_span: 24h
                    span:
                      start: hour
                    now:
                      show: true
                    apex_config:
                      chart:
                        height: 240
                      legend:
                        show: false
                      grid:
                        yaxis:
                          lines:
                            show: false
                      xaxis:
                        crosshairs:
                          show: false
                        tooltip:
                          enabled: false
                      yaxis:
                        - id: speed
                          title:
                            text: Wind Speed (km/h)
                          opposite: false
                        - id: direction
                          title:
                            text: Direction (°)
                          opposite: true
                          min: 0
                          max: 360
                    series:
                      - entity: sensor.met_no_hourly_forecasts_full_hourly_forecast
                        name: Speed
                        type: line
                        curve: smooth
                        yaxis_id: speed
                        color: "#0288D1"
                        stroke_width: 2
                        data_generator: |
                          return entity.attributes.forecasts.map(f => {
                            let speed = f.wind_speed;
                            speed = Math.round(speed * 10) / 10; // round to 1 decimal place
                            return { x: new Date(f.datetime).getTime(), y: speed };
                          });
                      - entity: sensor.met_no_hourly_forecasts_full_hourly_forecast
                        name: Direction
                        type: line
                        curve: smooth
                        yaxis_id: direction
                        color: "#8E24AA"
                        stroke_width: 1
                        data_generator: |
                          return entity.attributes.forecasts.map(f => {
                            let direction = Math.round(f.wind_bearing); // round to nearest integer
                            return { x: new Date(f.datetime).getTime(), y: direction };
                          });
      - type: grid
        cards:
          - type: vertical-stack
            cards:
              - type: custom:decluttering-card
                template: mushroom_title_subtitle_card
                variables:
                  - title: Radar
                  - subtitle: >-
                      {% set weather = states.weather.forecast_home.attributes
                      %}{% set temp = weather.temperature %}{% set temp_unit =
                      weather.temperature_unit %}{% set humidity =
                      weather.humidity %}{% set cloud = weather.cloud_coverage
                      %}{% set uv = weather.uv_index %}{% set pressure =
                      weather.pressure %}{% set wind_speed = weather.wind_speed
                      %}{% set wind_unit = weather.wind_speed_unit %}🌡️ {{ temp
                      }}{{ temp_unit }} 💧 Humidity: {{ humidity }}% ☁️ Clouds:
                      {{ cloud | round }}% 💨 Wind: {{ wind_speed }} {{
                      wind_unit }} 🌤️ UV index: {{ uv }}
              - type: custom:mod-card
                card:
                  type: iframe
                  url: >-
                    https://embed.windy.com/embed2.html?lat=your_lat&lon=your_long&width=800&height=600
                style: |
                  ha-card {
                    padding: 0;
                    height: 600px !important;   /* fix height */
                    width: 100% !important;
                    overflow: hidden;
                  }
                  iframe {
                    width: 100% !important;
                    height: 100% !important;
                  }
    cards: []
    badges:
      - type: custom:mushroom-chips-card
        chips:
          - type: template
            tap_action:
              action: navigate
              navigation_path: /family-foyer/overview
            icon: mdi:arrow-left
            card_mod:
              style:
                .: |
                  ha-card {
                    --color: {{ 'var(--md-sys-color-on-primary)'  }};
                    --chip-background: {{ 'var(--md-sys-color-primary)' }};
                    margin-top: 0.7em;
                  }
    header:
      layout: responsive
      badges_position: top
      badges_wrap: wrap
    background:
      opacity: 40
      alignment: center
      size: cover
      repeat: repeat
      attachment: fixed
      image: /api/image/serve/fcc922c73fe69b2ee6bc6fc24d7ebe44/original
    subview: true
  - type: sections
    max_columns: 3
    icon: mdi:camera-outline
    path: camera
    title: Camera
    sections:
      - type: grid
        cards:
          - type: vertical-stack
            cards:
              - type: horizontal-stack
                cards:
                  - type: custom:decluttering-card
                    template: mushroom_title_card
                    variables:
                      - title: Driveway
                  - type: custom:mushroom-chips-card
                    chips:
                      - type: template
                        entity: automation.right_driveway_camera_vehicle_notification
                        icon: >
                          {{ 'mdi:car' if
                          is_state('automation.right_driveway_camera_vehicle_notification',
                          'on') else 'mdi:car-off' }}
                        tap_action:
                          action: toggle
                      - type: template
                        entity: automation.right_driveway_camera_person_notification
                        icon: >
                          {{ 'mdi:motion-sensor' if
                          is_state('automation.right_driveway_camera_person_notification',
                          'on') else 'mdi:motion-sensor-off' }}
                        tap_action:
                          action: toggle
                    alignment: end
                    card_mod:
                      style: |
                        ha-card {
                          margin-top: 1.6em
                        }
              - type: vertical-stack
                cards:
                  - type: custom:advanced-camera-card
                    cameras:
                      - camera_entity: camera.driveway_medium_resolution_channel
                  - type: custom:mushroom-chips-card
                    alignment: start
                    chips:
                      - type: template
                        entity: switch.driveway_lights
                        icon: mdi:light-recessed
                        tap_action:
                          action: toggle
                        content: Driveway Light
                        card_mod:
                          style:
                            .: |
                              ha-card {
                                --text-color: {{ 'var(--primary-background-color)' if is_state('switch.light_switch_drw', 'on') else 'none' }};
                                --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.light_switch_drw', 'on') else 'none' }};
                                --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.light_switch_drw', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                              }
                      - type: template
                        entity: switch.porch_lights
                        icon: mdi:outdoor-lamp
                        tap_action:
                          action: toggle
                        content: Porch Light
                        card_mod:
                          style:
                            .: |
                              ha-card {
                                --text-color: {{ 'var(--primary-background-color)' if is_state('switch.porch_light_drw', 'on') else 'none' }};
                                --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.porch_light_drw', 'on') else 'none' }};
                                --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.porch_light_drw', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                              }
              - type: custom:button-card
                show_name: false
                show_icon: false
                tap_action:
                  action: none
                styles:
                  card:
                    - background: none
                    - box-shadow: none
                    - height: 50px
              - type: horizontal-stack
                cards:
                  - type: custom:decluttering-card
                    template: mushroom_title_card
                    variables:
                      - title: Backyard
                  - type: custom:mushroom-chips-card
                    chips:
                      - type: template
                        entity: binary_sensor.backyard_motion
                        icon: >
                          {{ 'mdi:motion-sensor' if
                          is_state('binary_sensor.backyard_motion', 'on') else
                          'mdi:motion-sensor-off' }}
                        tap_action:
                          action: toggle
                    alignment: end
                    card_mod:
                      style: |
                        ha-card {
                          margin-top: 1.6em
                        }
              - type: vertical-stack
                cards:
                  - type: custom:advanced-camera-card
                    cameras:
                      - camera_entity: camera.backyard_medium_resolution_channel
                  - type: custom:mushroom-chips-card
                    alignment: start
                    chips:
                      - type: template
                        entity: switch.patio_main_switch_la
                        icon: mdi:light-recessed
                        tap_action:
                          action: toggle
                        content: Main Patio Light
                        card_mod:
                          style:
                            .: |
                              ha-card {
                                --text-color: {{ 'var(--primary-background-color)' if is_state('switch.patio_main_switch_la', 'on') else 'none' }};
                                --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.patio_main_switch_la', 'on') else 'none' }};
                                --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.patio_main_switch_la', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                              }
      - type: grid
        cards:
          - type: vertical-stack
            cards:
              - type: horizontal-stack
                cards:
                  - type: custom:decluttering-card
                    template: mushroom_title_card
                    variables:
                      - title: Right Walkway
                  - type: custom:mushroom-chips-card
                    chips:
                      - type: template
                        entity: automation.right_walkway_camera_person_notification
                        icon: >
                          {{ 'mdi:motion-sensor' if
                          is_state('automation.right_walkway_camera_person_notification',
                          'on') else 'mdi:motion-sensor-off' }}
                        tap_action:
                          action: toggle
                    alignment: end
                    card_mod:
                      style: |
                        ha-card {
                          margin-top: 1.6em
                        }
              - type: vertical-stack
                cards:
                  - type: custom:webrtc-camera
                    streams:
                      - entity: camera.right_walkway_camera_fluent
                        mode: webrtc
                        muted: true
                  - type: custom:mushroom-chips-card
                    alignment: start
                    chips:
                      - type: template
                        entity: switch.patio_main_switch_la
                        icon: mdi:light-recessed
                        tap_action:
                          action: toggle
                        card_mod:
                          style:
                            .: |
                              ha-card {
                                --text-color: {{ 'var(--primary-background-color)' if is_state('switch.patio_main_switch_la', 'on') else 'none' }};
                                --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.patio_main_switch_la', 'on') else 'none' }};
                                --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.patio_main_switch_la', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                              }  
                        content: Main Patio Light
                      - type: template
                        entity: switch.patio_side_switch_la
                        icon: mdi:outdoor-lamp
                        tap_action:
                          action: toggle
                        content: Side Patio Light
                        card_mod:
                          style:
                            .: |
                              ha-card {
                                --text-color: {{ 'var(--primary-background-color)' if is_state('switch.patio_side_switch_la', 'on') else 'none' }};
                                --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.patio_side_switch_la', 'on') else 'none' }};
                                --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.patio_side_switch_la', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                              }
              - type: custom:button-card
                show_name: false
                show_icon: false
                tap_action:
                  action: none
                styles:
                  card:
                    - background: none
                    - box-shadow: none
                    - height: 50px
              - type: horizontal-stack
                cards:
                  - type: custom:decluttering-card
                    template: mushroom_title_card
                    variables:
                      - title: Garage
                  - type: custom:mushroom-chips-card
                    chips:
                      - type: template
                        entity: automation.garage_camera_person_notification
                        icon: >
                          {{ 'mdi:motion-sensor' if
                          is_state('automation.garage_camera_person_notification',
                          'on') else 'mdi:motion-sensor-off' }}
                        tap_action:
                          action: toggle
                    alignment: end
                    card_mod:
                      style: |
                        ha-card {
                          margin-top: 1.6em
                        }
              - type: vertical-stack
                cards:
                  - type: custom:advanced-camera-card
                    cameras:
                      - camera_entity: camera.garage_camera_fluent
                    dimensions:
                      aspect_ratio_mode: static
                      height: "300"
                  - type: custom:mushroom-chips-card
                    alignment: start
                    chips:
                      - type: template
                        entity: switch.light_switch_grg
                        icon: mdi:garage-variant
                        tap_action:
                          action: toggle
                        content: Garage Light
                        card_mod:
                          style:
                            .: |
                              ha-card {
                                --text-color: {{ 'var(--primary-background-color)' if is_state('switch.light_switch_grg', 'on') else 'none' }};
                                --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.light_switch_grg', 'on') else 'none' }};
                                --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.light_switch_grg', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                              }
                      - type: template
                        entity: switch.rear_light_switch_grg
                        icon: mdi:outdoor-lamp
                        tap_action:
                          action: toggle
                        content: Rear Garage Light
                        card_mod:
                          style:
                            .: |
                              ha-card {
                                --text-color: {{ 'var(--primary-background-color)' if is_state('switch.rear_light_switch_grg', 'on') else 'none' }};
                                --color: {{ 'var(--md-sys-color-on-primary)' if is_state('switch.rear_light_switch_grg', 'on') else 'none' }};
                                --chip-background: {{ 'var(--md-sys-color-primary)' if is_state('switch.rear_light_switch_grg', 'on') else 'color-mix(in srgb, var(--md-sys-color-surface-variant) 40%, transparent)' }};
                              }
    badges:
      - type: custom:mushroom-chips-card
        chips:
          - type: template
            tap_action:
              action: navigate
              navigation_path: /family-foyer/overview
            icon: mdi:arrow-left
            card_mod:
              style:
                .: |
                  ha-card {
                    --color: {{ 'var(--md-sys-color-on-primary)'  }};
                    --chip-background: {{ 'var(--md-sys-color-primary)' }};
                    margin-top: 0.7em;
                  }
    header:
      layout: responsive
      badges_position: top
      badges_wrap: wrap
    cards: []
    background:
      opacity: 30
      alignment: center
      size: cover
      repeat: repeat
      attachment: fixed
      image: /api/image/serve/fcc922c73fe69b2ee6bc6fc24d7ebe44/original
    subview: true
